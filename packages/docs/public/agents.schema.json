{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/custardcream98/relay/agents.schema.json",
  "title": "relay agents.pool.yml",
  "description": "Configuration schema for relay agent pool personas",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "language": {
      "type": "string",
      "description": "Default response language for all agents (e.g. 'Korean', 'English', 'Japanese'). Can be overridden per agent."
    },
    "agents": {
      "type": "object",
      "description": "Agent persona overrides. Keys are agent IDs.",
      "additionalProperties": {
        "$ref": "#/$defs/AgentConfig"
      }
    },
    "workflow": {
      "$ref": "#/$defs/WorkflowConfig"
    }
  },
  "required": ["agents"],
  "$defs": {
    "AgentConfig": {
      "type": "object",
      "description": "Agent configuration.",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name shown in the dashboard"
        },
        "emoji": {
          "type": "string",
          "description": "Emoji avatar for the agent"
        },
        "description": {
          "type": "string",
          "description": "One-line description of the agent's role"
        },
        "tools": {
          "type": "array",
          "description": "MCP tool names this agent is allowed to call",
          "items": {
            "type": "string",
            "enum": [
              "send_message",
              "get_messages",
              "create_task",
              "update_task",
              "get_my_tasks",
              "post_artifact",
              "get_artifact",
              "request_review",
              "submit_review",
              "read_memory",
              "write_memory",
              "append_memory",
              "save_session_summary",
              "list_sessions",
              "get_session_summary",
              "list_agents",
              "get_workflow"
            ]
          }
        },
        "systemPrompt": {
          "type": "string",
          "description": "System prompt injected into the agent at session start"
        },
        "language": {
          "type": "string",
          "description": "Override the global language for this specific agent (e.g. 'English')"
        },
        "disabled": {
          "type": "boolean",
          "description": "When true, this agent is excluded from the registry entirely"
        },
        "extends": {
          "type": "string",
          "description": "Inherit all fields from another agent ID, then apply overrides"
        }
      }
    },
    "WorkflowConfig": {
      "type": "object",
      "description": "Workflow job graph override. Merged at the job level with the default workflow.",
      "additionalProperties": false,
      "properties": {
        "jobs": {
          "type": "object",
          "description": "Job definitions keyed by job ID. Use '_done' as a terminal target in end maps.",
          "additionalProperties": {
            "$ref": "#/$defs/WorkflowJob"
          }
        }
      },
      "required": ["jobs"]
    },
    "WorkflowJob": {
      "type": "object",
      "description": "A single step in the workflow DAG",
      "additionalProperties": false,
      "properties": {
        "agents": {
          "type": "array",
          "description": "Agent IDs to run in parallel for this job",
          "items": { "type": "string" }
        },
        "description": {
          "type": "string",
          "description": "Natural-language job description injected into each agent's system prompt"
        },
        "end": {
          "type": "object",
          "description": "Map of next-job-id → condition string. Use '_done' to end the session.",
          "additionalProperties": { "type": "string" }
        },
        "reviewers": {
          "type": "object",
          "description": "Map of working-agent-id → list of reviewer agent IDs",
          "additionalProperties": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    }
  }
}
