---
title: Customising agents
description: "Learn how to define agent personas, roles, and tools in the agent pool — create any team for any domain: web dev, research, marketing, legal, or custom workflows."
---

Agent personas in relay are defined in an agent pool. Each `/relay:relay` session selects a task-optimised team from the pool via conversational team composition.

## File structure

```
your-project/
└── .relay/
    └── agents.pool.yml   # your agent pool (edit this file)
```

Copy `agents.pool.example.yml` (shipped with relay) to `.relay/agents.pool.yml` to get started.

## Basic structure of agents.pool.yml

```yaml
agents:
  fe:
    name: "Frontend Engineer"
    emoji: "🎨"
    description: "React, TypeScript, and UI implementation"
    tags: [frontend, react, typescript]
    tools:
      - get_messages
      - get_my_tasks
      - claim_task
      - update_task
      - post_artifact
      - send_message
      - request_review
    systemPrompt: |
      You are a Frontend Engineer specialising in React and TypeScript.
      ...
```

## extends

Inherit and extend another agent's configuration. This is useful for running multiple instances of the same role:

```yaml
agents:
  fe:
    name: "Frontend Engineer"
    emoji: "🎨"
    tags: [frontend]
    tools: [claim_task, get_my_tasks, update_task, post_artifact, send_message]
    systemPrompt: |
      You are a Frontend Engineer specialising in React and TypeScript.
      ...

  fe2:
    extends: fe          # inherit from the fe persona
    name: "Frontend Engineer (Reviewer)"
    emoji: "🔍"
```

## tags field

Tags are used by `/relay:relay` for smart team suggestion matching. Add tags that describe the agent's domain and skills:

```yaml
agents:
  security:
    name: "Security Engineer"
    emoji: "🔐"
    tags: [security, owasp, vulnerability]
    description: "Security vulnerability analysis and remediation"
    tools:
      - get_messages
      - get_my_tasks
      - update_task
      - get_artifact
      - post_artifact
      - send_message
    systemPrompt: |
      You are a security expert.
      Review code against the OWASP Top 10 and
      identify vulnerabilities with recommended fixes.
```

## tools field

Specifies the list of MCP tools the agent is allowed to call:

```yaml
tools:
  - send_message
  - get_messages
  - create_task
  - update_task
  - get_my_tasks
  - post_artifact
  - get_artifact
  - request_review
  - submit_review
  - read_memory
  - write_memory
  - append_memory
```

An agent cannot call any MCP tool not listed in its `tools` array.
It is recommended to grant only the minimum tools necessary for each role.
