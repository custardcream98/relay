# Dependencies
node_modules/

# Build output
packages/dashboard/dist/
packages/docs/dist/
packages/docs/.astro/

# Runtime
*.db
*.db-journal
*.db-shm
*.db-wal

# Ephemeral per-session team files — written by /relay:relay during Team Composition
.relay/session-agents*.yml

# Session data — ephemeral, stored in SQLite; not committed to git
.relay/sessions/

# Git worktrees
.worktrees/

# Local Claude Code settings (blocks project-level .mcp.json relay server during dev)
.claude/settings.local.json

# Environment variables
.env
.env.local

# OS
.DS_Store
/docs/
packages/docs/node_modules/
storybook-static/
